import { useState } from "react";
import { Grid, Typography } from "@mui/material";

const Admin = () => {
    return (
        <Grid align = 'center'>
            
        </Grid>
    );
}

export default Admin;